import * as SVG from "svgs"
import React from "react"

const StartupScreen = props => (
	<div className="flex flex-row justify-center items-center h-full bg-gray-50">
		<SVG.CodexLogo className="w-10 h-10 text-md-gray-400" />
	</div>
)

export default StartupScreen
